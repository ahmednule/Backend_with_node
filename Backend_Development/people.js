const people = ['Harry', 'brian', 'denzel', 'brenda', 'lilian', 'daisy']; // --apples

const ages = [12, 14, 16, 22, 35]; // -- maize

const sex = ['male', 'female'];  // --beans

console.log(people);

module.exports = {
    people, ages
};